{% extends "venue/base.html.twig" %}

{% block innercontent %}
    {{ include('venue/_details_navigation.html.twig', {venue: venue, show: 'contacts'}) }}

    <h5 class="pt-3">Kontakt für {{ venue.name }} bearbeiten</h5>

    {{ form(form) }}
    {{ form(deleteForm) }}
{% endblock %}
