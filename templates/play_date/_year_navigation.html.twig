<nav class="navbar">
    <ul class="nav nav-tabs">
        <a name="year-navigation" />
        {% for year in years %}
            <li class="nav-item">
                <a class="nav-link {{ activeYear == year ? 'active' }}" {{ activeYear == year ? 'aria-current="page"' }} href="{{ path('play_date_index', {year: year}) }}">
                    {{ year }}
                </a>
            </li>
        {% endfor %}
    </ul>
</nav>
